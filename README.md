# Local Traders SpA v. Gate.io — Case Record

A factual, source-linked documentation page covering:

- The 2023 cyberattack and blockchain forensic trace
- Four final Chilean court orders (RUC 2300614628-7 / RIT 8295-2024)
- Chile's CMF regulatory warning and Bank of Lithuania confirmation
- Apple's App Store removal
- Independent media coverage (La Tercera, Criptonoticias, Biobío Chile, El Mostrador)
- Gate's own self-published licenses, sourced directly from gate.com

## Files

- `index.html` — the page
- `style.css` — all styling
- `script.js` — live day-counter and scroll animations

## Running locally

No build step required. Open `index.html` directly in a browser, or serve the folder:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Deploying with GitHub Pages

1. Push this folder to a GitHub repository.
2. In the repo, go to **Settings → Pages**.
3. Under **Source**, select the branch (usually `main`) and root folder (`/`).
4. Save. GitHub will publish at `https://<your-username>.github.io/<repo-name>/`.

## Maintained by

Local Traders SpA · RUT 77.603.426-6 · contact@localtraders.finance
